import React from "react";
import PropTypes from "prop-types";
import BSOverlay from "react-bootstrap/Overlay";

const Overlay = (props) => {
  return <BSOverlay {...props} />;
};

export default Overlay;
